apiVersion: v1
kind: Service
metadata:
  name: {{ include "chips-staging.fullname" . }}
  labels:
    {{- include "chips-staging.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "chips-staging.selectorLabels" . | nindent 4 }}
